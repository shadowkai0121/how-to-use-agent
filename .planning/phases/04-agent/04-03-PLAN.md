---
phase: 04-agent
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - book/manifest.json
  - book/chapters/05-做出 FV 計算機的互動畫面.md
  - book/chapters/06-完成可驗證的 FV 計算機.md
  - book/chapters/07-把成果發布到 GitHub Pages.md
  - README.md
autonomous: false
requirements:
  - TUTO-01
  - TUTO-02
  - TUTO-05
must_haves:
  truths:
    - "第 5 到第 7 章把 Agent 協作主線一路帶到互動畫面、驗證完成與 GitHub Pages 發布。"
    - "root README 與 EPUB 會同步反映 Phase 4 完成後的章節摘要，而不是只剩舊骨架。"
    - "至少完成一次 fresh-clone walkthrough，確認 copyable prompt / command / checkpoint 真的能照著走。"
  artifacts:
    - path: "book/chapters/05-做出 FV 計算機的互動畫面.md"
      provides: "UI build tutorial step"
      contains: "## Prompt"
    - path: "book/chapters/06-完成可驗證的 FV 計算機.md"
      provides: "verification tutorial step"
      contains: "## Checkpoint"
    - path: "book/chapters/07-把成果發布到 GitHub Pages.md"
      provides: "deployment tutorial step"
      contains: "GitHub Pages"
    - path: "README.md"
      provides: "generated entry synced with phase 4 canonical content"
      contains: "## 章節索引"
  key_links:
    - from: "book/manifest.json"
      to: "book/chapters/05-做出 FV 計算機的互動畫面.md, book/chapters/06-完成可驗證的 FV 計算機.md, book/chapters/07-把成果發布到 GitHub Pages.md"
      via: "summary and checkpoint alignment"
      pattern: "github-pages-published"
    - from: "README.md"
      to: "book/frontmatter/00-前言.md, book/chapters/*.md"
      via: "generated chapter index and summaries"
      pattern: "build-readme"
---

<objective>
完成第 5 到第 7 章、同步 README / EPUB，並做一次 fresh-clone walkthrough 驗證，確認 Agent 協作主線真的可以沿章節走到可發布成果。

Purpose: 讓 Phase 4 不只在檔案結構上完整，還能證明讀者真的可以照著 prompt、command、checkpoint 前進。
Output: 第 5 到第 7 章、更新後的 `book/manifest.json`、重新生成的 `README.md`、fresh-clone walkthrough checkpoint
</objective>

<execution_context>
@C:/Users/asus/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/asus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-agent/04-CONTEXT.md
@.planning/phases/04-agent/04-RESEARCH.md
@.planning/phases/04-agent/04-VALIDATION.md
@.planning/phases/04-agent/04-01-PLAN.md
@.planning/phases/04-agent/04-02-PLAN.md
@book/manifest.json
@book/chapters/05-做出 FV 計算機的互動畫面.md
@book/chapters/06-完成可驗證的 FV 計算機.md
@book/chapters/07-把成果發布到 GitHub Pages.md
@scripts/build-readme.mjs
@scripts/build-epub.mjs
@scripts/verify-phase4.mjs
@scripts/verify-drift.mjs
@tests/phase-04/chapter-loop-contract.test.mjs
@tests/phase-04/copyable-steps.test.mjs
@tests/phase-04/checkpoint-recovery.test.mjs

<interfaces>
For this plan, the back-half tutorial contract must use these concrete chapter outcomes:

```text
第 5 章 -> 讀者用 Agent 協作做出 FV 計算機的互動畫面
第 6 章 -> 讀者完成可驗證的 FV 計算機，並用結果 + repo 證據驗證
第 7 章 -> 讀者把成果發布到 GitHub Pages，並取得可分享網址
```

The chapter-specific required strings are:

```text
book/chapters/05-做出 FV 計算機的互動畫面.md
- must contain `index.html`
- must contain `styles.css`
- must contain `main.js`

book/chapters/06-完成可驗證的 FV 計算機.md
- must contain `FV`
- must contain `範例輸入`
- must contain `你會看到正確結果`

book/chapters/07-把成果發布到 GitHub Pages.md
- must contain `GitHub Pages`
- must contain `Actions`
- must contain `公開網址`
```

The final generation / verification contract is fixed as:

```text
1. npm run build:readme
2. npm run build:epub
3. node scripts/verify-phase4.mjs
4. node --test tests/phase-04/*.test.mjs
5. node scripts/verify-drift.mjs
```

The fresh-clone walkthrough must check chapters 1, 3, 5, and 7 in order.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 完成第 5 到第 7 章的實作、驗收與部署教學 loop</name>
  <files>book/manifest.json, book/chapters/05-做出 FV 計算機的互動畫面.md, book/chapters/06-完成可驗證的 FV 計算機.md, book/chapters/07-把成果發布到 GitHub Pages.md</files>
  <read_first>
    .planning/phases/04-agent/04-CONTEXT.md
    .planning/phases/04-agent/04-RESEARCH.md
    .planning/phases/04-agent/04-02-PLAN.md
    book/manifest.json
    book/chapters/05-做出 FV 計算機的互動畫面.md
    book/chapters/06-完成可驗證的 FV 計算機.md
    book/chapters/07-把成果發布到 GitHub Pages.md
    scripts/verify-phase4.mjs
  </read_first>
  <action>更新 `book/chapters/05-做出 FV 計算機的互動畫面.md`、`book/chapters/06-完成可驗證的 FV 計算機.md`、`book/chapters/07-把成果發布到 GitHub Pages.md`，三章都必須採用 Phase 4 固定六段 heading。第 5 章要在 `## 本章產出` 清楚寫讀者會建立 `index.html`, `styles.css`, `main.js` 三個檔案的互動畫面骨架；`## Prompt` 引導 Agent 只先做本章 UI 與互動骨架，不提前處理部署；`## Command` 至少包含與本章相關的檔案檢查或 build 指令。第 6 章要在 `## Prompt` 要求 Agent 協助補齊 `FV` 計算邏輯與驗證步驟，在 `## Checkpoint` 第一條寫出 `你會看到正確結果` 與至少一組 `範例輸入`；第二條提供 repo 證據，例如比對 `main.js`、測試或 README / build 輸出。第 7 章要在 `## Prompt` 與 `## Command` 明確出現 `GitHub Pages`, `Actions`, `公開網址`，並把部署後的可觀察結果寫進 `## Checkpoint` 第一條。三章的 `## 卡關先這樣回正` 都要要求 Agent 退回上一個 checkpoint、保留本章檔案、只補剩下步驟。同步更新 `book/manifest.json` 中第 5 到第 7 章的 `summary` 與 `checkpoint`，讓 summary 能反映 UI、驗證、部署三個結果，checkpoint 則維持 `你應該` 開頭。</action>
  <acceptance_criteria>
    `book/chapters/05-做出 FV 計算機的互動畫面.md` 含有字串 `index.html`, `styles.css`, `main.js`
    `book/chapters/06-完成可驗證的 FV 計算機.md` 含有字串 `FV`, `範例輸入`, `你會看到正確結果`
    `book/chapters/07-把成果發布到 GitHub Pages.md` 含有字串 `GitHub Pages`, `Actions`, `公開網址`
    三個檔案都依序含有 `## 本章產出`, `## 你會練到什麼`, `## Prompt`, `## Command`, `## Checkpoint`, `## 卡關先這樣回正`
    `book/manifest.json` 含有更新後第 5 到第 7 章的 `summary` / `checkpoint`，且 `checkpoint` 以 `你應該` 開頭
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-phase4.mjs && node --test tests/phase-04/*.test.mjs</automated>
  </verify>
  <done>第 5 到第 7 章已把 Agent 協作主線一路帶到 UI、驗證與部署，不再只是骨架章節。</done>
</task>

<task type="auto">
  <name>Task 2: 重新生成 README / EPUB 並完成 Phase 4 全驗證</name>
  <files>README.md</files>
  <read_first>
    book/manifest.json
    book/frontmatter/00-前言.md
    book/chapters/01-建立你的閱讀與第一個實作起點.md
    book/chapters/02-準備你的 Agent 工作台與成果倉庫.md
    book/chapters/03-用 antigravity 對齊範圍與下一步.md
    book/chapters/04-用 GSD 拆出可執行的 phase.md
    book/chapters/05-做出 FV 計算機的互動畫面.md
    book/chapters/06-完成可驗證的 FV 計算機.md
    book/chapters/07-把成果發布到 GitHub Pages.md
    scripts/build-readme.mjs
    scripts/build-epub.mjs
    scripts/verify-phase4.mjs
    scripts/verify-drift.mjs
    README.md
  </read_first>
  <action>依 `<interfaces>` 的固定順序執行 `npm run build:readme`, `npm run build:epub`, `node scripts/verify-phase4.mjs`, `node --test tests/phase-04/*.test.mjs`, `node scripts/verify-drift.mjs`。若 `README.md` 需要更新，保留 generated artifact 變更。確認生成後的 `README.md` 章節索引與摘要已反映第 1 到第 7 章完整教學主線，而不是只有舊的 seed summary；README 仍必須保留 Phase 3 的 section headings 與 generated comment，不得手工插入第二套說明。若任一步驟失敗，就在修正後重跑同一順序直到全綠。</action>
  <acceptance_criteria>
    `README.md` 仍以 `<!-- GENERATED FILE. Source: book/manifest.json + book/frontmatter/00-前言.md + templates/README.template.md -->` 開頭
    `README.md` 含有 `## 章節索引`
    `README.md` 的章節索引與摘要已涵蓋第 1 到第 7 章主線，而不是 seed placeholder 文字
    `node scripts/verify-phase4.mjs` exits 0
    `node --test tests/phase-04/*.test.mjs` exits 0
    `node scripts/verify-drift.mjs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build:readme && npm run build:epub && node scripts/verify-phase4.mjs && node --test tests/phase-04/*.test.mjs && node scripts/verify-drift.mjs</automated>
  </verify>
  <done>README / EPUB 已與 canonical 教學主線同步，Phase 4 自動化 gate 全綠。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 用 fresh-clone walkthrough 驗證可複製教學主線</name>
  <files>book/frontmatter/00-前言.md, book/chapters/01-建立你的閱讀與第一個實作起點.md, book/chapters/03-用 antigravity 對齊範圍與下一步.md, book/chapters/05-做出 FV 計算機的互動畫面.md, book/chapters/07-把成果發布到 GitHub Pages.md</files>
  <read_first>
    book/frontmatter/00-前言.md
    book/chapters/01-建立你的閱讀與第一個實作起點.md
    book/chapters/03-用 antigravity 對齊範圍與下一步.md
    book/chapters/05-做出 FV 計算機的互動畫面.md
    book/chapters/07-把成果發布到 GitHub Pages.md
    README.md
  </read_first>
  <action>在乾淨 repo 狀態或 fresh clone 中，依順序走第 1、3、5、7 章。每一章都必須完整做一次 `複製 Prompt -> 執行 Command -> 比對 Checkpoint`，若流程偏掉，就立刻使用該章 `## 卡關先這樣回正` 內的 recovery prompt 測試是否能回到正軌。這個 walkthrough 的最低通過標準是：第 1 章能讓讀者知道起點與成果；第 3 章能把本次任務縮到下一個最小步驟；第 5 章能做出 FV 計算機互動畫面骨架；第 7 章能看到 GitHub Pages / 公開網址的部署結果或明確到達部署前最後一步。若任何章的 prompt、command、checkpoint 或 recovery 失效，就記錄是哪一章、哪一段、實際錯誤與預期差距。</action>
  <acceptance_criteria>
    第 1 章的 Prompt / Command / Checkpoint 能在 fresh clone 中照著走
    第 3 章的 Prompt 能讓 Agent 只列出接下來最小步驟，而不是跨章改檔
    第 5 章的流程能帶出 FV 計算機互動畫面骨架
    第 7 章的流程能抵達 GitHub Pages 部署結果或取得公開網址步驟
    若故意讓 Agent 偏掉，對應章節的 recovery prompt 能把流程拉回上一個 checkpoint
  </acceptance_criteria>
  <what-built>已完成第 1 到第 7 章的 Agent 協作教學主線、Phase 4 gate、README / EPUB 同步與 automated verification。這一步只確認真人照著章節操作時也能成立。</what-built>
  <verify>
    <automated>npm run check:phase-04</automated>
  </verify>
  <how-to-verify>
    1. 在乾淨 repo 或 fresh clone 中，先讀前言與第 1 章。
    2. 照第 1 章複製 Prompt，執行該章 Command，確認 Checkpoint 成立。
    3. 跳到第 3 章，確認 Agent 只列出接下來最小步驟，沒有直接跨章修改。
    4. 照第 5 章做出 FV 計算機互動畫面骨架，確認本章 Checkpoint。
    5. 照第 7 章完成 GitHub Pages 部署結果或取得公開網址步驟，確認本章 Checkpoint。
    6. 在其中一章故意讓 Agent 做超出範圍的事，測試 recovery prompt 是否能把流程拉回上一個 checkpoint。
  </how-to-verify>
  <resume-signal>Type "approved" if chapters 1, 3, 5, and 7 all work as a copyable walkthrough, or report exactly which chapter section failed and what the mismatch was.</resume-signal>
  <done>使用者已確認 fresh-clone walkthrough 可以照章節主線前進，或已明確指出哪一章需要補救。</done>
</task>

</tasks>

<verification>
先執行 `npm run build:readme && npm run build:epub && node scripts/verify-phase4.mjs && node --test tests/phase-04/*.test.mjs && node scripts/verify-drift.mjs`，再進行 fresh-clone walkthrough。這個 plan 的完成標準包含自動化與真人 walkthrough 兩條驗證。
</verification>

<success_criteria>
第 5 到第 7 章已完成，README / EPUB 已同步，且至少一次 fresh-clone walkthrough 證明整條 Agent 協作教學主線真的可複製。
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent/04-agent-03-SUMMARY.md`
</output>
