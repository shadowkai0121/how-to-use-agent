---
phase: 04-agent
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - book/manifest.json
  - book/frontmatter/00-前言.md
  - book/chapters/01-建立你的閱讀與第一個實作起點.md
  - book/chapters/02-準備你的 Agent 工作台與成果倉庫.md
  - book/chapters/03-用 antigravity 對齊範圍與下一步.md
  - book/chapters/04-用 GSD 拆出可執行的 phase.md
autonomous: true
requirements:
  - TUTO-01
  - TUTO-02
  - TUTO-05
must_haves:
  truths:
    - "讀者從前言到第 4 章就能掌握本書的閱讀方式、Agent 工作台、範圍對齊方式，以及 phase 拆解節奏。"
    - "前半段教學已經把 Prompt、Command、Checkpoint、Recovery 的節奏做成固定套路。"
    - "manifest 的 summary / checkpoint 與章節正文保持同一條主線，不會一邊寫正文一邊讓入口摘要漂移。"
  artifacts:
    - path: "book/frontmatter/00-前言.md"
      provides: "book-level collaboration framing"
      contains: "## 本章產出"
    - path: "book/chapters/03-用 antigravity 對齊範圍與下一步.md"
      provides: "scope-alignment tutorial chapter"
      contains: "## Prompt"
    - path: "book/chapters/04-用 GSD 拆出可執行的 phase.md"
      provides: "phase breakdown tutorial chapter"
      contains: "## 卡關先這樣回正"
    - path: "book/manifest.json"
      provides: "canonical summaries and checkpoints aligned with front-half tutorial flow"
      contains: "\"align-scope-with-antigravity\""
  key_links:
    - from: "book/manifest.json"
      to: "book/frontmatter/00-前言.md, book/chapters/01-建立你的閱讀與第一個實作起點.md, book/chapters/02-準備你的 Agent 工作台與成果倉庫.md, book/chapters/03-用 antigravity 對齊範圍與下一步.md, book/chapters/04-用 GSD 拆出可執行的 phase.md"
      via: "summary and checkpoint alignment"
      pattern: "checkpoint"
    - from: "book/chapters/03-用 antigravity 對齊範圍與下一步.md"
      to: "book/chapters/04-用 GSD 拆出可執行的 phase.md"
      via: "chapter handoff"
      pattern: "下一章"
---

<objective>
完成前言與第 1 到第 4 章的 Agent 協作教學主線，讓讀者先學會如何閱讀、如何搭工作台、如何用 antigravity 對齊範圍，以及如何用 GSD 的 phase 思維拆工作。

Purpose: 先把前半段的協作節奏寫完整，讓後半段 FV 計算機實作能承接同一套 tutorial loop。
Output: `book/frontmatter/00-前言.md`、第 1 到第 4 章、對應的 `book/manifest.json` 更新
</objective>

<execution_context>
@C:/Users/asus/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/asus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-agent/04-CONTEXT.md
@.planning/phases/04-agent/04-RESEARCH.md
@.planning/phases/04-agent/04-VALIDATION.md
@.planning/phases/04-agent/04-01-PLAN.md
@book/manifest.json
@book/frontmatter/00-前言.md
@book/chapters/01-建立你的閱讀與第一個實作起點.md
@book/chapters/02-準備你的 Agent 工作台與成果倉庫.md
@book/chapters/03-用 antigravity 對齊範圍與下一步.md
@book/chapters/04-用 GSD 拆出可執行的 phase.md
@scripts/verify-phase4.mjs
@tests/phase-04/chapter-loop-contract.test.mjs
@tests/phase-04/copyable-steps.test.mjs
@tests/phase-04/checkpoint-recovery.test.mjs

<interfaces>
For this plan, the front-half tutorial contract must use these concrete chapter outcomes:

```text
前言 -> 讀者理解本書的閱讀方式、Agent / Command 的分工、以及每章 checkpoint 的用途
第 1 章 -> 讀者能描述本書成果、知道從哪裡開始、並對齊第一個 Agent 任務
第 2 章 -> 讀者完成本機工作台與成果倉庫準備
第 3 章 -> 讀者用 antigravity 對齊本次任務範圍與下一步
第 4 章 -> 讀者把工作拆成可執行的 phases，而不是直接叫 Agent 一次做完
```

Each of the files in this plan must contain the exact six Phase 4 headings from Plan 01, plus these concrete chapter-specific strings:

```text
book/frontmatter/00-前言.md
- must contain `Agent 負責一起思考與修改內容`
- must contain `Command 只在終端機執行`

book/chapters/02-準備你的 Agent 工作台與成果倉庫.md
- must contain `git clone`
- must contain `npm install`

book/chapters/03-用 antigravity 對齊範圍與下一步.md
- must contain `antigravity`
- must contain `只列出接下來 3 個最小步驟`

book/chapters/04-用 GSD 拆出可執行的 phase.md
- must contain `phase`
- must contain `不要直接開始改檔`
```

The manifest updates for chapter 1-4 must keep summaries and checkpoints aligned with the rewritten prose, and each corresponding checkpoint string in `book/manifest.json` must start with `你應該`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重寫前言與第 1 到第 2 章為固定教學 loop</name>
  <files>book/manifest.json, book/frontmatter/00-前言.md, book/chapters/01-建立你的閱讀與第一個實作起點.md, book/chapters/02-準備你的 Agent 工作台與成果倉庫.md</files>
  <read_first>
    .planning/phases/04-agent/04-CONTEXT.md
    .planning/phases/04-agent/04-RESEARCH.md
    .planning/phases/04-agent/04-01-PLAN.md
    book/manifest.json
    book/frontmatter/00-前言.md
    book/chapters/01-建立你的閱讀與第一個實作起點.md
    book/chapters/02-準備你的 Agent 工作台與成果倉庫.md
    scripts/verify-phase4.mjs
  </read_first>
  <action>更新 `book/frontmatter/00-前言.md`、`book/chapters/01-建立你的閱讀與第一個實作起點.md` 與 `book/chapters/02-準備你的 Agent 工作台與成果倉庫.md`，把原本的 `## 本章會完成什麼` / `## 你會產出的東西` 結構改寫為 Phase 4 固定六段：`## 本章產出`, `## 你會練到什麼`, `## Prompt`, `## Command`, `## Checkpoint`, `## 卡關先這樣回正`。前言檔內要明確寫出 `Agent 負責一起思考與修改內容` 與 `Command 只在終端機執行`，並在 `## Prompt` 放一段 ```text block，內容要求 Agent 先說明這本書的閱讀方式與本章成果；在 `## Command` 放 ```bash block，至少包含 `git status`。第 1 章要把讀者帶到第一個具體任務：理解成果主線、確認從哪一章開始、並要求 Agent 只先整理本章最小步驟，不要預先完成第 2 章。第 2 章要在 `## Command` 放精確可複製指令 `git clone <repo-url>`、`cd how-to-use-agent`、`npm install`，並在 `## Checkpoint` 第一條寫讀者應該看到套件安裝成功或 repo 已可操作，第二條寫 `git status` 或目錄檢查證據。同步更新 `book/manifest.json` 中前言、第 1 章、第 2 章的 `summary` 與 `checkpoint`，讓三筆 `checkpoint` 字串都以 `你應該` 開頭，且內容與正文的本章產出一致。</action>
  <acceptance_criteria>
    `book/frontmatter/00-前言.md` 含有字串 `Agent 負責一起思考與修改內容` 與 `Command 只在終端機執行`
    `book/frontmatter/00-前言.md` 依序含有 `## 本章產出`, `## 你會練到什麼`, `## Prompt`, `## Command`, `## Checkpoint`, `## 卡關先這樣回正`
    `book/chapters/02-準備你的 Agent 工作台與成果倉庫.md` 含有字串 `git clone`, `cd how-to-use-agent`, `npm install`
    `book/manifest.json` 含有更新後第 1 章與第 2 章的 `summary` / `checkpoint`，且對應 `checkpoint` 以 `你應該` 開頭
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-phase4.mjs && node --test tests/phase-04/chapter-loop-contract.test.mjs tests/phase-04/checkpoint-recovery.test.mjs</automated>
  </verify>
  <done>前言到第 2 章已經形成穩定的教學入口，讀者知道如何分辨 Agent 與 Command，也完成工作台準備。</done>
</task>

<task type="auto">
  <name>Task 2: 完成第 3 到第 4 章的範圍對齊與 phase 拆解節奏</name>
  <files>book/manifest.json, book/chapters/03-用 antigravity 對齊範圍與下一步.md, book/chapters/04-用 GSD 拆出可執行的 phase.md</files>
  <read_first>
    .planning/phases/04-agent/04-CONTEXT.md
    .planning/phases/04-agent/04-RESEARCH.md
    book/manifest.json
    book/chapters/03-用 antigravity 對齊範圍與下一步.md
    book/chapters/04-用 GSD 拆出可執行的 phase.md
    scripts/verify-phase4.mjs
    tests/phase-04/copyable-steps.test.mjs
  </read_first>
  <action>更新 `book/chapters/03-用 antigravity 對齊範圍與下一步.md` 與 `book/chapters/04-用 GSD 拆出可執行的 phase.md`，兩章都必須改用 Phase 4 固定六段 heading。第 3 章的 `## Prompt` 必須直接出現 `antigravity` 與句子 `只列出接下來 3 個最小步驟`；`## Command` 至少放一組讀者用來閱讀 repo 或比對狀態的指令，例如 `git diff --stat` 或 `node scripts/verify-phase4.mjs`。第 4 章的 `## Prompt` 要引導 Agent 先把工作拆成 `phase`，並明確寫出 `不要直接開始改檔`；`## Command` 至少包含一次檢查 manifest、章節檔或 phase gate 的指令。兩章的 `## Checkpoint` 都要先寫結果，再寫 repo 證據，最後一句要明確說 `可以進到下一章` 或 `先不要往下`。兩章的 `## 卡關先這樣回正` 都要要求 Agent `退回上一個 checkpoint`、`保留` 指定章節檔，並重列剩下最小步驟。同步更新 `book/manifest.json` 中第 3 章與第 4 章的 `summary` 與 `checkpoint`，讓 manifest 的 summary 描述這兩章分別是 `用 antigravity 對齊範圍` 與 `拆出可執行 phases`，checkpoint 則維持 `你應該` 開頭。</action>
  <acceptance_criteria>
    `book/chapters/03-用 antigravity 對齊範圍與下一步.md` 含有字串 `antigravity` 與 `只列出接下來 3 個最小步驟`
    `book/chapters/04-用 GSD 拆出可執行的 phase.md` 含有字串 `phase` 與 `不要直接開始改檔`
    兩個檔案都依序含有 `## 本章產出`, `## 你會練到什麼`, `## Prompt`, `## Command`, `## Checkpoint`, `## 卡關先這樣回正`
    `book/manifest.json` 含有更新後第 3 章與第 4 章的 `summary` / `checkpoint`，且 `checkpoint` 以 `你應該` 開頭
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-phase4.mjs && node --test tests/phase-04/copyable-steps.test.mjs tests/phase-04/checkpoint-recovery.test.mjs</automated>
  </verify>
  <done>前半段主線已把「先對齊範圍，再拆 phase」的協作節奏寫進章節，讀者可帶著同一套路進入實作章。</done>
</task>

</tasks>

<verification>
執行 `node scripts/verify-phase4.mjs && node --test tests/phase-04/*.test.mjs`。這個 plan 的完成標準是前言與第 1 到第 4 章都符合固定 tutorial loop，而且 manifest 摘要 / checkpoint 已同步。
</verification>

<success_criteria>
前言與前四章已形成可重複的 Agent 協作節奏，讀者能理解本書閱讀方式、完成工作台準備、對齊範圍、並拆出可執行 phases。
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent/04-agent-02-SUMMARY.md`
</output>
