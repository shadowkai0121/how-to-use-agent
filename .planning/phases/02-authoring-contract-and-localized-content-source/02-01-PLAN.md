---
phase: 02-authoring-contract-and-localized-content-source
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - book/manifest.schema.json
  - scripts/validate-authoring.mjs
  - tests/phase-02/helpers.mjs
  - tests/phase-02/content-tree.test.mjs
  - tests/phase-02/gfm-contract.test.mjs
  - tests/phase-02/placeholders.test.mjs
autonomous: true
requirements:
  - CONT-01
  - CONT-03
  - CONT-04
must_haves:
  truths:
    - "維護者有一個可機器檢查的作者寫作契約，不需要靠口頭約定記住內容樹、GFM 安全子集與 placeholder 規則。"
    - "Phase 2 的內容樹、GFM 規範與待補圖格式都有對應自動化測試，後續改動可以立即失敗。"
  artifacts:
    - path: "scripts/validate-authoring.mjs"
      provides: "作者內容規範檢查入口"
      contains: "assets/placeholders/"
    - path: "tests/phase-02/content-tree.test.mjs"
      provides: "內容樹與 manifest 路徑驗證"
      contains: "book/frontmatter"
    - path: "tests/phase-02/gfm-contract.test.mjs"
      provides: "GFM 安全寫作契約驗證"
      contains: "heading level jump"
  key_links:
    - from: "scripts/validate-authoring.mjs"
      to: "book/manifest.json"
      via: "manifest file path and metadata checks"
      pattern: "book/manifest.json"
    - from: "tests/phase-02/*.test.mjs"
      to: "book/**/*.md"
      via: "authoring contract assertions"
      pattern: "book/"
---

<objective>
建立 Phase 2 的基礎契約與驗證腳手架：擴充 manifest schema、加入作者內容驗證腳本，並建立覆蓋內容樹 / GFM 安全規範 / placeholder 規則的測試檔。

Purpose: 先把內容來源邊界與機器檢查規則鎖定，後續建立中文檔案與術語表時不需要再猜結構或驗證方式。
Output: `package.json`、`book/manifest.schema.json`、`scripts/validate-authoring.mjs`、`tests/phase-02/*.test.mjs`
</objective>

<execution_context>
@C:/Users/asus/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/asus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-authoring-contract-and-localized-content-source/02-CONTEXT.md
@.planning/phases/02-authoring-contract-and-localized-content-source/02-RESEARCH.md
@.planning/phases/02-authoring-contract-and-localized-content-source/02-VALIDATION.md
@.planning/phases/01-reader-entry-and-chapter-topology/01-01-PLAN.md
@.planning/phases/01-reader-entry-and-chapter-topology/01-02-PLAN.md

<interfaces>
This plan must lock these concrete authoring contract values:

```json
{
  "language": "zh-TW",
  "frontmatter": [
    {
      "id": "preface",
      "order": 0,
      "file": "book/frontmatter/00-前言.md",
      "title": "前言",
      "summary": "說明這本書的目標、讀者對象與學習方式。",
      "checkpoint": "你已理解本書的定位與閱讀方式。",
      "milestone": null,
      "authorNotes": ["保留為 README / EPUB 共用入口內容"],
      "outputs": ["readme", "epub"]
    }
  ],
  "chapters[*].file": "book/chapters/01-建立你的閱讀與第一個實作起點.md",
  "appendices[*].file": "book/appendices/附錄-a-術語表.md"
}
```

Allowed extended syntax is limited to:

```markdown
> [!NOTE]
> 說明文字

<details>
<summary>展開看完整說明</summary>

補充內容

</details>
```

Placeholder images must use this exact shape:

```markdown
![這裡預計放 GitHub Pages 部署完成畫面的截圖](../assets/placeholders/ch07-github-pages-live.png)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 擴充 package 與 manifest schema 以支援 canonical 內容樹</name>
  <files>package.json, book/manifest.schema.json</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/02-authoring-contract-and-localized-content-source/02-CONTEXT.md
    .planning/phases/02-authoring-contract-and-localized-content-source/02-RESEARCH.md
    .planning/phases/01-reader-entry-and-chapter-topology/01-01-PLAN.md
    package.json
    book/manifest.schema.json
  </read_first>
  <action>更新 `package.json`。保留 Phase 1 既有 `type: "module"` 與既有 scripts，新增或覆寫 scripts 為 `"validate:authoring": "node scripts/validate-authoring.mjs"`, `"test:phase-02": "node --test tests/phase-02/*.test.mjs"`, `"check:phase-02": "npm run validate:manifest && npm run validate:authoring && npm run test:phase-02 && npm run lint:md"`；devDependencies 至少包含 `"ajv": "8.18.0"`, `"markdownlint-cli2": "0.22.0"`, `"prettier": "3.8.1"`, `"yaml": "2.8.3"`。更新 `book/manifest.schema.json` 為 JSON Schema Draft 2020-12，頂層 required 欄位固定為 `bookTitle`, `bookSlug`, `language`, `frontmatter`, `chapters`, `appendices`, `milestones`；`language` const 固定為 `zh-TW`。建立共用 `documentEntry` schema，required 欄位固定為 `id`, `order`, `file`, `title`, `summary`, `checkpoint`, `milestone`, `authorNotes`, `outputs`；`file` pattern 固定為 `^book/(frontmatter|chapters|appendices)/.+\\.md$`；`outputs` items enum 固定為 `readme`, `epub`；`authorNotes` type 固定為 string array；`milestone` enum 固定為 `null`, `fv-calculator-complete`, `github-pages-published`。`frontmatter`, `chapters`, `appendices` 都要引用同一份 `documentEntry` 定義，且 `chapters` 保留給 1 到 7 的主章節，`frontmatter` 與 `appendices` 供 Phase 2 起的額外內容使用。</action>
  <acceptance_criteria>
    `package.json` 含有字串 `"validate:authoring": "node scripts/validate-authoring.mjs"`
    `package.json` 含有字串 `"test:phase-02": "node --test tests/phase-02/*.test.mjs"`
    `package.json` 含有字串 `"yaml": "2.8.3"`
    `book/manifest.schema.json` 含有字串 `"language"` 與 `"const": "zh-TW"`
    `book/manifest.schema.json` 含有字串 `"required": ["bookTitle", "bookSlug", "language", "frontmatter", "chapters", "appendices", "milestones"]`
    `book/manifest.schema.json` 含有字串 `"pattern": "^book/(frontmatter|chapters|appendices)/.+\\\\.md$"`
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$pkg = Get-Content -Raw 'package.json' | ConvertFrom-Json; if ($pkg.scripts.'validate:authoring' -ne 'node scripts/validate-authoring.mjs') { throw 'missing validate:authoring' }; if ($pkg.scripts.'test:phase-02' -ne 'node --test tests/phase-02/*.test.mjs') { throw 'missing test:phase-02' }; if ($pkg.scripts.'check:phase-02' -ne 'npm run validate:manifest && npm run validate:authoring && npm run test:phase-02 && npm run lint:md') { throw 'missing check:phase-02' }; if ($pkg.devDependencies.yaml -ne '2.8.3') { throw 'yaml version mismatch' }; $schemaText = Get-Content -Raw 'book/manifest.schema.json'; if ($schemaText -notmatch '\"const\": \"zh-TW\"') { throw 'schema language const missing' }; if ($schemaText -notmatch 'book/\\(frontmatter\\|chapters\\|appendices\\)') { throw 'schema file pattern missing' }"</automated>
  </verify>
  <done>Phase 2 已有明確的 manifest 結構與 package 入口，後續內容樹與作者檔案都能被同一組 schema 接受。</done>
</task>

<task type="auto">
  <name>Task 2: 建立作者內容驗證腳本與三個需求測試檔</name>
  <files>scripts/validate-authoring.mjs, tests/phase-02/helpers.mjs, tests/phase-02/content-tree.test.mjs, tests/phase-02/gfm-contract.test.mjs, tests/phase-02/placeholders.test.mjs</files>
  <read_first>
    .planning/phases/02-authoring-contract-and-localized-content-source/02-RESEARCH.md
    .planning/phases/02-authoring-contract-and-localized-content-source/02-VALIDATION.md
    package.json
    book/manifest.schema.json
    scripts/validate-authoring.mjs
    tests/phase-02/helpers.mjs
    tests/phase-02/content-tree.test.mjs
    tests/phase-02/gfm-contract.test.mjs
    tests/phase-02/placeholders.test.mjs
  </read_first>
  <action>建立 `tests/phase-02/helpers.mjs`，匯出 `readText(path)`, `readJson(path)`, `allMarkdownFiles()`, `extractStandaloneImages(text)`, `extractHeadings(text)` 五個 helper。建立 `scripts/validate-authoring.mjs`，腳本必須讀取 `book/manifest.json`，檢查 `frontmatter[*].file` 都位於 `book/frontmatter/`、`chapters[*].file` 都位於 `book/chapters/`、`appendices[*].file` 都位於 `book/appendices/`；檢查所有 manifest file 路徑對應到真實檔案；檢查每個 Markdown 檔第一個 heading 必須是唯一 H1；檢查 heading 不能跳階，例如 `##` 之後不可直接出現 `####`；檢查所有 Markdown 連結只允許相對路徑或 `https://` / `http://` 絕對 URL；檢查 `<details>` 只允許與 `<summary>` 成對出現；檢查所有指向 `assets/placeholders/` 的圖片都必須是獨立一行的 Markdown 圖片語法。建立 `tests/phase-02/content-tree.test.mjs`，驗證 manifest 含 `frontmatter`, `chapters`, `appendices` 三個陣列，且至少包含 `book/frontmatter/00-前言.md`, `book/chapters/01-建立你的閱讀與第一個實作起點.md`, `book/appendices/附錄-a-術語表.md` 三條路徑。建立 `tests/phase-02/gfm-contract.test.mjs`，驗證 `scripts/validate-authoring.mjs` 會拒絕 heading 跳階、非相對/HTTP 連結、缺少 `<summary>` 的 `<details>`，並以 fixture strings 覆蓋 `> [!NOTE]` 與 fenced code block 為允許語法。建立 `tests/phase-02/placeholders.test.mjs`，驗證 placeholder 圖片路徑必須含 `assets/placeholders/`、必須是獨立 block、不得出現在一般句子中。</action>
  <acceptance_criteria>
    `scripts/validate-authoring.mjs` 含有字串 `book/frontmatter/`, `book/chapters/`, `book/appendices/`, `assets/placeholders/`
    `scripts/validate-authoring.mjs` 含有字串 `<details>` 與 `<summary>`
    `tests/phase-02/helpers.mjs` 含有字串 `export function allMarkdownFiles` 與 `export function extractStandaloneImages`
    `tests/phase-02/content-tree.test.mjs` 含有字串 `book/frontmatter/00-前言.md` 與 `book/appendices/附錄-a-術語表.md`
    `tests/phase-02/gfm-contract.test.mjs` 含有字串 `> [!NOTE]` 與 `heading level jump`
    `tests/phase-02/placeholders.test.mjs` 含有字串 `assets/placeholders/` 與 `standalone`
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "node --check 'scripts/validate-authoring.mjs'; $files = 'tests/phase-02/helpers.mjs','tests/phase-02/content-tree.test.mjs','tests/phase-02/gfm-contract.test.mjs','tests/phase-02/placeholders.test.mjs'; foreach ($file in $files) { node --check $file }"</automated>
  </verify>
  <done>Phase 2 的內容樹、GFM 安全規範與 placeholder 規則已經有可執行的驗證入口與對應測試檔。</done>
</task>

</tasks>

<verification>
執行 `node --check scripts/validate-authoring.mjs` 與 `node --check tests/phase-02/*.test.mjs`。這個 plan 只要求腳手架可解析，不要求測試綠燈，因為真實內容檔會在下一個 plan 建立。
</verification>

<success_criteria>
Phase 2 已有明確的 schema、作者驗證腳本與三個測試檔，後續內容樹與 Markdown 範例可直接對照固定規則實作。
</success_criteria>

<output>
After completion, create `.planning/phases/02-authoring-contract-and-localized-content-source/02-authoring-contract-and-localized-content-source-01-SUMMARY.md`
</output>
