---
phase: 03-dual-output-build-and-publish-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - scripts/lib/phase3-tooling.mjs
  - tests/phase-03/helpers.mjs
  - tests/phase-03/readme-generation.test.mjs
  - tests/phase-03/epub-build.test.mjs
  - tests/phase-03/epub-validation.test.mjs
  - tests/phase-03/link-and-asset-validation.test.mjs
  - tests/phase-03/placeholder-gate.test.mjs
  - tests/phase-03/drift-detection.test.mjs
autonomous: true
requirements:
  - PUBL-01
  - PUBL-02
  - PUBL-04
must_haves:
  truths:
    - "維護者有固定的 Phase 3 指令入口，可以一致地建置 README、EPUB 與三層驗證。"
    - "README 生成、EPUB 建置、EPUB 驗證、連結檢查、placeholder gate 與 drift 檢查都有對應的自動化測試骨架。"
    - "維護者在本機與 CI 都能用同一組 Phase 3 指令解析 Pandoc、Java、EPUBCheck 與 lychee，不需要手動猜測環境變數或預設路徑。"
  artifacts:
    - path: "package.json"
      provides: "Phase 3 script contracts and Node 24 runtime floor"
      contains: "\"verify:phase3:release\": \"node scripts/verify-phase3.mjs --tier release\""
    - path: "scripts/lib/phase3-tooling.mjs"
      provides: "tool resolution helpers for pandoc, java, epubcheck, lychee"
      contains: "EPUBCHECK_JAR"
    - path: "tests/phase-03/readme-generation.test.mjs"
      provides: "PUBL-01 build contract assertions"
      contains: "## 章節索引"
    - path: "tests/phase-03/epub-validation.test.mjs"
      provides: "PUBL-03 validation contract assertions"
      contains: "java -jar"
  key_links:
    - from: "package.json"
      to: "scripts/build-readme.mjs, scripts/build-epub.mjs, scripts/verify-phase3.mjs"
      via: "named npm scripts"
      pattern: "build:readme"
    - from: "tests/phase-03/*.test.mjs"
      to: "book/manifest.json"
      via: "canonical source expectations"
      pattern: "book/manifest.json"
    - from: "scripts/lib/phase3-tooling.mjs"
      to: "pandoc, java, epubcheck, lychee"
      via: "environment-variable-first tool lookup"
      pattern: "PANDOC_BIN"
---

<objective>
建立 Phase 3 的執行契約：固定 npm scripts、集中式工具解析 helper，以及六個需求對應的測試骨架，讓後續 build/verify 實作可以直接對著明確 interface 落地。

Purpose: 先把 README/EPUB/validation 的入口與測試邊界鎖死，避免後續 plan 在檔名、命令、tier 名稱或環境變數上各自發明一套。
Output: `package.json`、`scripts/lib/phase3-tooling.mjs`、`tests/phase-03/*.test.mjs`
</objective>

<execution_context>
@C:/Users/asus/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/asus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-dual-output-build-and-publish-validation/03-CONTEXT.md
@.planning/phases/03-dual-output-build-and-publish-validation/03-RESEARCH.md
@.planning/phases/03-dual-output-build-and-publish-validation/03-VALIDATION.md
@.planning/phases/02-authoring-contract-and-localized-content-source/02-02-PLAN.md
@.planning/phases/02-authoring-contract-and-localized-content-source/02-03-PLAN.md
@book/manifest.json
@book/frontmatter/00-前言.md

<interfaces>
Phase 3 must standardize on these exact command names and tier names in `package.json`:

```json
{
  "build:readme": "node scripts/build-readme.mjs",
  "build:epub": "node scripts/build-epub.mjs",
  "test:phase-03": "node --test tests/phase-03/*.test.mjs",
  "verify:drift": "node scripts/verify-drift.mjs",
  "verify:links:local": "node scripts/verify-links.mjs --tier local",
  "verify:links:pr": "node scripts/verify-links.mjs --tier pr",
  "verify:links:release": "node scripts/verify-links.mjs --tier release",
  "verify:placeholders": "node scripts/verify-placeholders.mjs",
  "verify:epub": "node scripts/verify-epub.mjs",
  "verify:phase3:local": "node scripts/verify-phase3.mjs --tier local",
  "verify:phase3:pr": "node scripts/verify-phase3.mjs --tier pr",
  "verify:phase3:release": "node scripts/verify-phase3.mjs --tier release"
}
```

The README generator must produce these exact section headings:

```markdown
## 這本書的目的
## 適合誰閱讀
## 開始閱讀
## 章節索引
## 里程碑提示
## 取得 EPUB
## 發布與驗證狀態
```

The EPUB pipeline must target this exact output file and metadata values:

```yaml
output: dist/epub/how-to-use-agent.epub
snapshot: dist/epub/how-to-use-agent.snapshot.json
title: Agent 使用電子書
language: zh-TW
```

The tooling helper must resolve these exact environment variables before falling back to default binaries or paths:

```text
PANDOC_BIN -> pandoc
JAVA_BIN -> java
EPUBCHECK_JAR -> tools/epubcheck/epubcheck.jar
LYCHEE_BIN -> lychee
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 鎖定 Phase 3 指令介面與工具解析規則</name>
  <files>package.json, scripts/lib/phase3-tooling.mjs</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/03-dual-output-build-and-publish-validation/03-CONTEXT.md
    .planning/phases/03-dual-output-build-and-publish-validation/03-RESEARCH.md
    .planning/phases/03-dual-output-build-and-publish-validation/03-VALIDATION.md
    package.json
    scripts/lib/phase3-tooling.mjs
  </read_first>
  <action>更新 `package.json`，保留 Phase 1/2 既有 scripts，並新增或覆寫以下 Phase 3 scripts 為精確字串：`"build:readme": "node scripts/build-readme.mjs"`, `"build:epub": "node scripts/build-epub.mjs"`, `"test:phase-03": "node --test tests/phase-03/*.test.mjs"`, `"verify:drift": "node scripts/verify-drift.mjs"`, `"verify:links:local": "node scripts/verify-links.mjs --tier local"`, `"verify:links:pr": "node scripts/verify-links.mjs --tier pr"`, `"verify:links:release": "node scripts/verify-links.mjs --tier release"`, `"verify:placeholders": "node scripts/verify-placeholders.mjs"`, `"verify:epub": "node scripts/verify-epub.mjs"`, `"verify:phase3:local": "node scripts/verify-phase3.mjs --tier local"`, `"verify:phase3:pr": "node scripts/verify-phase3.mjs --tier pr"`, `"verify:phase3:release": "node scripts/verify-phase3.mjs --tier release"`, `"check:phase-03": "npm run test:phase-03 && npm run verify:phase3:local"`。同時把 `engines.node` 固定為 `>=24.0.0`。建立 `scripts/lib/phase3-tooling.mjs`，匯出 `resolvePandocBin()`, `resolveJavaBin()`, `resolveEpubcheckJar()`, `resolveLycheeBin()`, `assertFileExists(path, label)` 五個函式；解析邏輯必須先讀 `PANDOC_BIN`, `JAVA_BIN`, `EPUBCHECK_JAR`, `LYCHEE_BIN`，若未提供則分別退回 `pandoc`, `java`, `tools/epubcheck/epubcheck.jar`, `lychee`；若 `tools/epubcheck/epubcheck.jar` 不存在，錯誤訊息必須明確寫出 `Set EPUBCHECK_JAR or place epubcheck.jar at tools/epubcheck/epubcheck.jar`。</action>
  <acceptance_criteria>
    `package.json` 含有字串 `"build:readme": "node scripts/build-readme.mjs"`
    `package.json` 含有字串 `"verify:phase3:release": "node scripts/verify-phase3.mjs --tier release"`
    `package.json` 含有字串 `"check:phase-03": "npm run test:phase-03 && npm run verify:phase3:local"`
    `package.json` 含有字串 `"node": ">=24.0.0"`
    `scripts/lib/phase3-tooling.mjs` 含有字串 `PANDOC_BIN`, `JAVA_BIN`, `EPUBCHECK_JAR`, `LYCHEE_BIN`
    `scripts/lib/phase3-tooling.mjs` 含有字串 `tools/epubcheck/epubcheck.jar`
    `scripts/lib/phase3-tooling.mjs` 含有字串 `Set EPUBCHECK_JAR or place epubcheck.jar at tools/epubcheck/epubcheck.jar`
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$pkg = Get-Content -Raw 'package.json' | ConvertFrom-Json; if ($pkg.scripts.'build:readme' -ne 'node scripts/build-readme.mjs') { throw 'missing build:readme' }; if ($pkg.scripts.'build:epub' -ne 'node scripts/build-epub.mjs') { throw 'missing build:epub' }; if ($pkg.scripts.'test:phase-03' -ne 'node --test tests/phase-03/*.test.mjs') { throw 'missing test:phase-03' }; if ($pkg.scripts.'verify:phase3:release' -ne 'node scripts/verify-phase3.mjs --tier release') { throw 'missing verify:phase3:release' }; if ($pkg.scripts.'check:phase-03' -ne 'npm run test:phase-03 && npm run verify:phase3:local') { throw 'missing check:phase-03' }; if ($pkg.engines.node -ne '>=24.0.0') { throw 'node engine mismatch' }; node --check 'scripts/lib/phase3-tooling.mjs'"</automated>
  </verify>
  <done>Phase 3 後續所有 build 與 verify plan 都有固定 command contract 與共同工具解析 helper，可直接依此實作。</done>
</task>

<task type="auto">
  <name>Task 2: 建立 README、EPUB 與 validation 的六個測試骨架</name>
  <files>tests/phase-03/helpers.mjs, tests/phase-03/readme-generation.test.mjs, tests/phase-03/epub-build.test.mjs, tests/phase-03/epub-validation.test.mjs, tests/phase-03/link-and-asset-validation.test.mjs, tests/phase-03/placeholder-gate.test.mjs, tests/phase-03/drift-detection.test.mjs</files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/phases/03-dual-output-build-and-publish-validation/03-CONTEXT.md
    .planning/phases/03-dual-output-build-and-publish-validation/03-RESEARCH.md
    .planning/phases/03-dual-output-build-and-publish-validation/03-VALIDATION.md
    book/manifest.json
    book/frontmatter/00-前言.md
    tests/phase-03/helpers.mjs
    tests/phase-03/readme-generation.test.mjs
    tests/phase-03/epub-build.test.mjs
    tests/phase-03/epub-validation.test.mjs
    tests/phase-03/link-and-asset-validation.test.mjs
    tests/phase-03/placeholder-gate.test.mjs
    tests/phase-03/drift-detection.test.mjs
  </read_first>
  <action>建立 `tests/phase-03/helpers.mjs`，匯出 `readText(path)`, `readJson(path)`, `makeTempDir(prefix)`, `writeFixtureBook(root, overrides)`, `runScript(entry, args = [])`, `assertContainsAll(text, needles)` 六個 helper。建立 `tests/phase-03/readme-generation.test.mjs`，要求 `scripts/build-readme.mjs` 生成的 `README.md` 必須含七個固定 section headings：`## 這本書的目的`, `## 適合誰閱讀`, `## 開始閱讀`, `## 章節索引`, `## 里程碑提示`, `## 取得 EPUB`, `## 發布與驗證狀態`，且章節索引需使用 `book/manifest.json` 的 `chapters[*].title` 與 `summary`。建立 `tests/phase-03/epub-build.test.mjs`，要求 `scripts/build-epub.mjs` 與 `pandoc/defaults/epub.yaml` 以 `book/frontmatter/00-前言.md`、manifest chapter order、`book/appendices/附錄-a-術語表.md` 作為來源，並輸出 `dist/epub/how-to-use-agent.epub` 與 `dist/epub/how-to-use-agent.snapshot.json`。建立 `tests/phase-03/drift-detection.test.mjs`，要求 snapshot key 精確包含 `chapterOrder`, `chapterTitles`, `chapterSummaries`, `milestones`, `epubAccess`, `entrySections`，且 `scripts/verify-drift.mjs` 要在 `README drift`, `EPUB drift`, `README stale` 三種情境輸出可讀標題。建立 `tests/phase-03/link-and-asset-validation.test.mjs`，要求 `scripts/verify-links.mjs` 支援 `--tier local`, `--tier pr`, `--tier release` 三個值；`local` 檢查 `README.md`、`book/**/*.md` 與相對資產存在性；`pr` 追加完整外部 URL 掃描；`release` 追加對 fresh rebuild 產物的檢查。建立 `tests/phase-03/placeholder-gate.test.mjs`，要求 `scripts/verify-placeholders.mjs` 掃描 `README.md`, `book/frontmatter/**/*.md`, `book/chapters/**/*.md`, `book/appendices/**/*.md` 與 built output，並對 `assets/placeholders/`, `../assets/placeholders/`, `[[IMAGE_REQUEST:`, `placeholder[-_/]` 直接 fail。建立 `tests/phase-03/epub-validation.test.mjs`，要求 `scripts/verify-epub.mjs` 呼叫 `java -jar`, 讀取 `EPUBCHECK_JAR`, 對 `dist/epub/how-to-use-agent.epub` 執行 EPUBCheck，並把 `WARNING` 與 `ERROR` 都視為硬阻擋。所有測試檔都使用 `node:test` 與 fixture content tree，不直接依賴真實 repo 目前是否已替換 placeholder。</action>
  <acceptance_criteria>
    `tests/phase-03/helpers.mjs` 含有字串 `export function readText`, `export function writeFixtureBook`, `export async function runScript`
    `tests/phase-03/readme-generation.test.mjs` 含有字串 `## 章節索引` 與 `## 發布與驗證狀態`
    `tests/phase-03/epub-build.test.mjs` 含有字串 `dist/epub/how-to-use-agent.epub` 與 `dist/epub/how-to-use-agent.snapshot.json`
    `tests/phase-03/drift-detection.test.mjs` 含有字串 `README drift`, `EPUB drift`, `README stale`
    `tests/phase-03/link-and-asset-validation.test.mjs` 含有字串 `--tier local`, `--tier pr`, `--tier release`
    `tests/phase-03/placeholder-gate.test.mjs` 含有字串 `assets/placeholders/`, `[[IMAGE_REQUEST:`, `placeholder[-_/]`
    `tests/phase-03/epub-validation.test.mjs` 含有字串 `java -jar`, `EPUBCHECK_JAR`, `WARNING`
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$files = 'tests/phase-03/helpers.mjs','tests/phase-03/readme-generation.test.mjs','tests/phase-03/epub-build.test.mjs','tests/phase-03/epub-validation.test.mjs','tests/phase-03/link-and-asset-validation.test.mjs','tests/phase-03/placeholder-gate.test.mjs','tests/phase-03/drift-detection.test.mjs'; foreach ($file in $files) { node --check $file }"</automated>
  </verify>
  <done>Phase 3 的六個 requirement behaviors 都有對應測試骨架與固定斷言字串，後續實作可直接用這些 tests 作為完成定義。</done>
</task>

</tasks>

<verification>
執行 `node --check scripts/lib/phase3-tooling.mjs` 與所有 `tests/phase-03/*.test.mjs`。本 plan 只要求 contracts 與測試骨架可解析，不要求 Phase 3 tests 綠燈，因為實際 build/verify scripts 會在後續 plans 建立。
</verification>

<success_criteria>
`package.json` 已提供固定的 Phase 3 commands，工具解析 helper 已建立，六個 README/EPUB/validation behaviors 都有具體測試骨架，後續 plans 不需要再決定 tier 名稱、output 檔名、snapshot key 或 verification command。
</success_criteria>

<output>
After completion, create `.planning/phases/03-dual-output-build-and-publish-validation/03-dual-output-build-and-publish-validation-01-SUMMARY.md`
</output>
