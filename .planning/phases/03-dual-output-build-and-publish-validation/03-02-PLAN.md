---
phase: 03-dual-output-build-and-publish-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - templates/README.template.md
  - scripts/lib/phase3-book.mjs
  - scripts/build-readme.mjs
  - scripts/build-epub.mjs
  - scripts/verify-drift.mjs
  - pandoc/defaults/epub.yaml
  - pandoc/metadata/epub.yaml
  - assets/styles/epub.css
  - README.md
autonomous: true
requirements:
  - PUBL-01
  - PUBL-02
  - QUAL-03
must_haves:
  truths:
    - "維護者可以從同一組 canonical `book/` 內容生成根目錄 `README.md`，且入口資訊與 manifest 同步。"
    - "維護者可以從同一組 canonical `book/` 內容生成可閱讀的 EPUB，包含 title、language、metadata、TOC 與文字型封面頁。"
    - "README 與 EPUB 的核心同步欄位有 machine-readable snapshot，可被 drift 驗證腳本直接比較。"
  artifacts:
    - path: "scripts/build-readme.mjs"
      provides: "README generator from canonical book content"
      contains: "## 章節索引"
    - path: "scripts/build-epub.mjs"
      provides: "Pandoc EPUB build orchestrator"
      contains: "dist/epub/how-to-use-agent.epub"
    - path: "pandoc/defaults/epub.yaml"
      provides: "versioned Pandoc defaults for EPUB"
      contains: "to: epub3"
    - path: "scripts/verify-drift.mjs"
      provides: "README and EPUB snapshot drift gate"
      contains: "README drift"
  key_links:
    - from: "scripts/lib/phase3-book.mjs"
      to: "book/manifest.json, book/frontmatter/00-前言.md"
      via: "canonical snapshot builder"
      pattern: "chapterSummaries"
    - from: "scripts/build-readme.mjs"
      to: "README.md"
      via: "template rendering from canonical snapshot"
      pattern: "README.template.md"
    - from: "scripts/build-epub.mjs"
      to: "pandoc/defaults/epub.yaml, pandoc/metadata/epub.yaml"
      via: "spawned pandoc build"
      pattern: "how-to-use-agent.epub"
---

<objective>
實作雙輸出生成層：把 canonical `book/` 內容接到根目錄 `README.md` 與 `dist/epub/how-to-use-agent.epub`，並建立供 drift gate 重用的 canonical snapshot。

Purpose: 讓 README 與 EPUB 都從同一組內容來源重建，徹底切斷手工維護根目錄 README 或另一套 EPUB 文案的可能性。
Output: `README.md`、README template、README/EPUB build scripts、Pandoc defaults/metadata、EPUB CSS、drift verifier
</objective>

<execution_context>
@C:/Users/asus/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/asus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-dual-output-build-and-publish-validation/03-CONTEXT.md
@.planning/phases/03-dual-output-build-and-publish-validation/03-RESEARCH.md
@.planning/phases/03-dual-output-build-and-publish-validation/03-VALIDATION.md
@.planning/phases/03-dual-output-build-and-publish-validation/03-01-PLAN.md
@book/manifest.json
@book/frontmatter/00-前言.md
@book/appendices/附錄-a-術語表.md
@package.json
@tests/phase-03/readme-generation.test.mjs
@tests/phase-03/epub-build.test.mjs
@tests/phase-03/drift-detection.test.mjs

<interfaces>
The shared canonical snapshot used by both builders and the drift check must include these exact keys:

```json
{
  "bookTitle": "Agent 使用電子書",
  "language": "zh-TW",
  "chapterOrder": [1, 2, 3, 4, 5, 6, 7],
  "chapterTitles": [],
  "chapterSummaries": [],
  "milestones": {
    "fv-calculator-complete": "finish-fv-calculator",
    "github-pages-published": "publish-github-pages"
  },
  "epubAccess": {
    "localFile": "dist/epub/how-to-use-agent.epub",
    "releaseLabel": "how-to-use-agent.epub"
  },
  "entrySections": {
    "purpose": "",
    "audience": "",
    "startReading": "",
    "publishStatus": ""
  }
}
```

The generated `README.md` must begin with this exact comment:

```markdown
<!-- GENERATED FILE. Source: book/manifest.json + book/frontmatter/00-前言.md + templates/README.template.md -->
```

The textual cover for EPUB must be a generated Markdown page written to `dist/epub/cover.md` with these lines near the top:

```markdown
# Agent 使用電子書
## Canonical EPUB Build
- 語言：zh-TW
- 來源：book/manifest.json
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 canonical snapshot、README template 與 root README 生成器</name>
  <files>templates/README.template.md, scripts/lib/phase3-book.mjs, scripts/build-readme.mjs, README.md</files>
  <read_first>
    .planning/phases/03-dual-output-build-and-publish-validation/03-CONTEXT.md
    .planning/phases/03-dual-output-build-and-publish-validation/03-RESEARCH.md
    .planning/phases/03-dual-output-build-and-publish-validation/03-01-PLAN.md
    book/manifest.json
    book/frontmatter/00-前言.md
    templates/README.template.md
    scripts/lib/phase3-book.mjs
    scripts/build-readme.mjs
    README.md
    tests/phase-03/readme-generation.test.mjs
  </read_first>
  <action>建立 `scripts/lib/phase3-book.mjs`，匯出 `loadCanonicalBook()` 與 `buildCanonicalSnapshot()`；它必須讀取 `book/manifest.json` 與 `book/frontmatter/00-前言.md`，產出 `<interfaces>` 指定的 snapshot keys，並從 manifest 依序抽出 chapter `order`, `title`, `summary`, `milestone`、以及 `milestones` 對應關係。建立 `templates/README.template.md`，保留固定 comment `<!-- GENERATED FILE. Source: book/manifest.json + book/frontmatter/00-前言.md + templates/README.template.md -->`，並精確包含以下 section headings：`## 這本書的目的`, `## 適合誰閱讀`, `## 開始閱讀`, `## 章節索引`, `## 里程碑提示`, `## 取得 EPUB`, `## 發布與驗證狀態`。其中 `## 章節索引` 區塊必須預留 `{{CHAPTER_INDEX}}` placeholder，格式固定為 `1. [第 1 章：建立你的閱讀與第一個實作起點](./book/chapters/01-建立你的閱讀與第一個實作起點.md) — 理解本書目標、成果與固定閱讀方式。`；`## 里程碑提示` 須預留 `{{MILESTONES}}`；`## 取得 EPUB` 必須寫出 `執行 npm run build:epub 後可取得 dist/epub/how-to-use-agent.epub` 與 `GitHub Release 下載檔名為 how-to-use-agent.epub`；`## 發布與驗證狀態` 必須寫出 `本檔由 canonical 內容生成，不接受手工編輯。`。建立 `scripts/build-readme.mjs`，以 `loadCanonicalBook()` + `buildCanonicalSnapshot()` 渲染 template，輸出 root `README.md`。README 的 `## 開始閱讀` 區塊必須精確產出 `[從前言開始](./book/frontmatter/00-前言.md)` 與 `[直接進入第 1 章](./book/chapters/01-建立你的閱讀與第一個實作起點.md)` 兩個入口。產生後的 `README.md` 必須整檔覆寫，不保留 Phase 1 的手工 auto markers。</action>
  <acceptance_criteria>
    `templates/README.template.md` 含有字串 `## 這本書的目的`, `## 適合誰閱讀`, `## 開始閱讀`, `## 章節索引`, `## 里程碑提示`, `## 取得 EPUB`, `## 發布與驗證狀態`
    `templates/README.template.md` 含有字串 `{{CHAPTER_INDEX}}` 與 `{{MILESTONES}}`
    `scripts/lib/phase3-book.mjs` 含有字串 `buildCanonicalSnapshot`, `chapterSummaries`, `epubAccess`, `entrySections`
    `scripts/build-readme.mjs` 含有字串 `README.template.md`, `book/frontmatter/00-前言.md`, `README.md`
    `README.md` 開頭含有字串 `<!-- GENERATED FILE. Source: book/manifest.json + book/frontmatter/00-前言.md + templates/README.template.md -->`
    `README.md` 含有字串 `[從前言開始](./book/frontmatter/00-前言.md)` 與 `[直接進入第 1 章](./book/chapters/01-建立你的閱讀與第一個實作起點.md)`
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/phase-03/readme-generation.test.mjs</automated>
  </verify>
  <done>root `README.md` 已完全改成 generated artifact，且入口資訊、章節索引、里程碑與 EPUB 取得方式都由 canonical source 重建。</done>
</task>

<task type="auto">
  <name>Task 2: 建立 Pandoc EPUB pipeline 與 README/EPUB drift gate</name>
  <files>scripts/build-epub.mjs, scripts/verify-drift.mjs, pandoc/defaults/epub.yaml, pandoc/metadata/epub.yaml, assets/styles/epub.css</files>
  <read_first>
    .planning/phases/03-dual-output-build-and-publish-validation/03-CONTEXT.md
    .planning/phases/03-dual-output-build-and-publish-validation/03-RESEARCH.md
    book/manifest.json
    book/frontmatter/00-前言.md
    book/appendices/附錄-a-術語表.md
    scripts/lib/phase3-book.mjs
    scripts/lib/phase3-tooling.mjs
    scripts/build-epub.mjs
    scripts/verify-drift.mjs
    pandoc/defaults/epub.yaml
    pandoc/metadata/epub.yaml
    assets/styles/epub.css
    tests/phase-03/epub-build.test.mjs
    tests/phase-03/drift-detection.test.mjs
  </read_first>
  <action>建立 `pandoc/metadata/epub.yaml`，精確包含 `title: Agent 使用電子書`, `lang: zh-TW`, `creator: Agent 使用電子書專案`, `identifier: urn:how-to-use-agent:epub`, `rights: Generated for repository publishing validation`。建立 `assets/styles/epub.css`，至少定義 `body`, `h1`, `h2`, `code`, `blockquote` 的基本排版，且不得引用外部字型或遠端資產。建立 `pandoc/defaults/epub.yaml`，精確包含 `from: gfm`, `to: epub3`, `standalone: true`, `table-of-contents: true`, `metadata-files: [pandoc/metadata/epub.yaml]`, `css: [assets/styles/epub.css]`, `resource-path: ['.', 'book', 'assets']`, `output-file: dist/epub/how-to-use-agent.epub`。建立 `scripts/build-epub.mjs`，它必須先用 `buildCanonicalSnapshot()` 產出 `dist/epub/how-to-use-agent.snapshot.json`，再生成 `dist/epub/cover.md` 作為文字型封面頁，內容前四行必須與 `<interfaces>` 相符；接著依順序把 `dist/epub/cover.md`, `book/frontmatter/00-前言.md`, `book/chapters/*.md`, `book/appendices/附錄-a-術語表.md` 傳給 Pandoc，輸出 `dist/epub/how-to-use-agent.epub`。建立 `scripts/verify-drift.mjs`，它必須重新計算 canonical snapshot，讀取 `README.md` 與 `dist/epub/how-to-use-agent.snapshot.json`，逐一比對 `chapterOrder`, `chapterTitles`, `chapterSummaries`, `milestones`, `epubAccess`, `entrySections`。若 `README.md` 不是用目前 template 和 snapshot 重新生成的內容，錯誤標題必須是 `README stale`；若 README 核心欄位不同步，錯誤標題必須是 `README drift`；若 EPUB snapshot 核心欄位不同步，錯誤標題必須是 `EPUB drift`；所有失敗都要印出對應欄位名稱與 expected / actual 摘要。</action>
  <acceptance_criteria>
    `pandoc/defaults/epub.yaml` 含有字串 `from: gfm`, `to: epub3`, `output-file: dist/epub/how-to-use-agent.epub`
    `pandoc/metadata/epub.yaml` 含有字串 `title: Agent 使用電子書`, `lang: zh-TW`, `creator: Agent 使用電子書專案`
    `assets/styles/epub.css` 含有字串 `body`, `h1`, `h2`, `code`, `blockquote`
    `scripts/build-epub.mjs` 含有字串 `dist/epub/how-to-use-agent.epub`, `dist/epub/how-to-use-agent.snapshot.json`, `dist/epub/cover.md`, `pandoc/defaults/epub.yaml`
    `scripts/verify-drift.mjs` 含有字串 `README stale`, `README drift`, `EPUB drift`
    `scripts/verify-drift.mjs` 含有字串 `chapterOrder`, `chapterTitles`, `chapterSummaries`, `entrySections`
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/phase-03/epub-build.test.mjs tests/phase-03/drift-detection.test.mjs</automated>
  </verify>
  <done>同一份 canonical `book/` 內容已能生成 `README.md` 與 EPUB，且 README/EPUB 核心同步欄位可被 drift gate 明確比較與阻擋。</done>
</task>

</tasks>

<verification>
執行 `node --test tests/phase-03/readme-generation.test.mjs` 與 `node --test tests/phase-03/epub-build.test.mjs tests/phase-03/drift-detection.test.mjs`。這個 plan 的完成標準是 builders、template、Pandoc defaults 與 drift verifier 都已存在並符合固定 contracts。
</verification>

<success_criteria>
README 與 EPUB 都已從 canonical `book/` 內容重建，根目錄 README 不再手工維護，EPUB 有固定 metadata / TOC / 文字型封面頁，且 drift check 能直接比較 README 與 EPUB 的核心同步欄位。
</success_criteria>

<output>
After completion, create `.planning/phases/03-dual-output-build-and-publish-validation/03-dual-output-build-and-publish-validation-02-SUMMARY.md`
</output>
