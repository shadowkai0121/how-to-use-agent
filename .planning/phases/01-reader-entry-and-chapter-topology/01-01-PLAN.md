---
phase: 01-reader-entry-and-chapter-topology
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - book/manifest.schema.json
  - scripts/validate-manifest.mjs
  - tests/phase-01/helpers.mjs
  - tests/phase-01/manifest-schema.test.mjs
  - tests/phase-01/topology-order.test.mjs
  - tests/phase-01/chapter-nav.test.mjs
  - tests/phase-01/milestone-signaling.test.mjs
  - tests/phase-01/readme-entry.test.mjs
autonomous: true
requirements:
  - CONT-02
must_haves:
  truths:
    - "維護者有一個可機器檢查的單一 manifest 契約，而不是分散在多個 Markdown 檔案中的順序規則。"
    - "Phase 1 的閱讀入口、章節順序、章尾導覽與 milestone 訊號都有自動檢查，後續變更不需要靠人工比對。"
  artifacts:
    - path: "book/manifest.schema.json"
      provides: "manifest 的唯一結構契約"
      contains: "\"required\": [\"bookTitle\", \"bookSlug\", \"chapters\", \"milestones\"]"
    - path: "scripts/validate-manifest.mjs"
      provides: "schema 與 topology 驗證入口"
      contains: "Ajv"
    - path: "tests/phase-01/manifest-schema.test.mjs"
      provides: "CONT-02 自動化驗證"
      contains: "manifest conforms to schema"
  key_links:
    - from: "scripts/validate-manifest.mjs"
      to: "book/manifest.schema.json"
      via: "JSON Schema Draft 2020-12 + Ajv"
      pattern: "manifest.schema.json"
    - from: "tests/phase-01/*.test.mjs"
      to: "book/manifest.json"
      via: "topology assertions"
      pattern: "book/manifest.json"
---

<objective>
建立 Phase 1 的機器可驗證契約：Node 腳本入口、manifest schema、以及覆蓋 README 入口 / 章節順序 / 章尾導覽 / milestone 訊號的測試骨架。

Purpose: 先把「唯一真相來源」與驗證邊界鎖定，後續撰寫 README 與章節時不需要再猜欄位、命名或檢查方式。
Output: `package.json`、`book/manifest.schema.json`、`scripts/validate-manifest.mjs`、`tests/phase-01/*.test.mjs`
</objective>

<execution_context>
@C:/Users/asus/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/asus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-reader-entry-and-chapter-topology/01-CONTEXT.md
@.planning/phases/01-reader-entry-and-chapter-topology/01-RESEARCH.md
@.planning/phases/01-reader-entry-and-chapter-topology/01-VALIDATION.md

<interfaces>
The manifest contract this plan establishes must include these concrete fields:

```json
{
  "$schema": "./manifest.schema.json",
  "bookTitle": "Agent 使用電子書",
  "bookSlug": "how-to-use-agent",
  "chapters": [
    {
      "id": "reader-entry",
      "order": 1,
      "file": "chapters/01-reader-entry-and-first-win.md",
      "title": "建立你的閱讀與第一個實作起點",
      "summary": "理解本書目標、成果與固定閱讀方式。",
      "checkpoint": "你已知道從哪裡開始、整條學習路徑會通往哪裡。",
      "milestone": null,
      "isFvMainline": true
    }
  ],
  "milestones": {
    "fv-calculator-complete": {
      "label": "完成 FV 計算機",
      "chapterId": "finish-fv-calculator"
    },
    "github-pages-published": {
      "label": "發布到 GitHub Pages",
      "chapterId": "publish-github-pages"
    }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 manifest 驗證契約與 Node 腳本入口</name>
  <files>package.json, book/manifest.schema.json, scripts/validate-manifest.mjs</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/01-reader-entry-and-chapter-topology/01-CONTEXT.md
    .planning/phases/01-reader-entry-and-chapter-topology/01-RESEARCH.md
    package.json
    book/manifest.schema.json
    scripts/validate-manifest.mjs
  </read_first>
  <action>建立 `package.json`，內容必須包含 `"name": "how-to-use-agent-book"`, `"private": true`, `"type": "module"`，以及 scripts: `"test": "node --test tests/phase-01/*.test.mjs"`, `"lint:md": "markdownlint-cli2 \"**/*.md\""`, `"validate:manifest": "node scripts/validate-manifest.mjs"`, `"sync:topology": "node scripts/sync-topology.mjs"`, `"check:phase-01": "npm run validate:manifest && npm run test && npm run lint:md"`；devDependencies 必須固定為 `ajv@8.18.0`, `markdownlint-cli2@0.22.0`, `prettier@3.8.1`。建立 `book/manifest.schema.json`，使用 JSON Schema Draft 2020-12，頂層 required 欄位固定為 `bookTitle`, `bookSlug`, `chapters`, `milestones`；`chapters[*]` required 欄位固定為 `id`, `order`, `file`, `title`, `summary`, `checkpoint`, `milestone`, `isFvMainline`；`file` pattern 固定為 `^chapters/[0-9]{2}-.*\\.md$`；`milestone` enum 固定為 `null`, `fv-calculator-complete`, `github-pages-published`。建立 `scripts/validate-manifest.mjs`，用 Ajv 讀取 schema 與 `book/manifest.json`，並在 schema 驗證之外額外檢查 `chapter.id`、`chapter.order`、`chapter.file` 唯一、章節 `order` 需從 1 開始連續遞增、`milestones.fv-calculator-complete.chapterId` 與 `milestones.github-pages-published.chapterId` 都必須存在於 `chapters[*].id`，以及 manifest 內每個 `file` 路徑都必須對應到真實檔案。錯誤輸出必須用 `console.error()` 列出具體欄位與失敗原因，並以 `process.exit(1)` 結束。</action>
  <acceptance_criteria>
    `package.json` 含有字串 `"check:phase-01": "npm run validate:manifest && npm run test && npm run lint:md"`
    `package.json` 含有字串 `"ajv": "8.18.0"`
    `book/manifest.schema.json` 含有字串 `"required": ["bookTitle", "bookSlug", "chapters", "milestones"]`
    `book/manifest.schema.json` 含有字串 `"enum": [null, "fv-calculator-complete", "github-pages-published"]`
    `scripts/validate-manifest.mjs` 同時含有字串 `new Ajv`, `manifest.schema.json`, `book/manifest.json`, `process.exit(1)`
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$pkg = Get-Content -Raw 'package.json' | ConvertFrom-Json; $schema = Get-Content -Raw 'book/manifest.schema.json' | ConvertFrom-Json; if ($pkg.name -ne 'how-to-use-agent-book') { throw 'package.json name mismatch' }; if (-not $pkg.private) { throw 'package.json private must be true' }; if ($pkg.type -ne 'module') { throw 'package.json type mismatch' }; $requiredScripts = @{ 'test' = 'node --test tests/phase-01/*.test.mjs'; 'lint:md' = 'markdownlint-cli2 \"**/*.md\"'; 'validate:manifest' = 'node scripts/validate-manifest.mjs'; 'sync:topology' = 'node scripts/sync-topology.mjs'; 'check:phase-01' = 'npm run validate:manifest && npm run test && npm run lint:md' }; foreach ($name in $requiredScripts.Keys) { if ($pkg.scripts.$name -ne $requiredScripts[$name]) { throw \"package.json script mismatch: $name\" } }; $requiredDeps = @{ 'ajv' = '8.18.0'; 'markdownlint-cli2' = '0.22.0'; 'prettier' = '3.8.1' }; foreach ($name in $requiredDeps.Keys) { if ($pkg.devDependencies.$name -ne $requiredDeps[$name]) { throw \"package.json devDependency mismatch: $name\" } }; $requiredFields = @('bookTitle','bookSlug','chapters','milestones'); foreach ($field in $requiredFields) { if (-not ($schema.required -contains $field)) { throw \"schema missing required field: $field\" } }; $chapterDef = $schema.properties.chapters.items; $requiredChapterFields = @('id','order','file','title','summary','checkpoint','milestone','isFvMainline'); foreach ($field in $requiredChapterFields) { if (-not ($chapterDef.required -contains $field)) { throw \"schema missing chapter required field: $field\" } }; if ($chapterDef.properties.file.pattern -ne '^chapters/[0-9]{2}-.*\\.md$') { throw 'schema file pattern mismatch' }; $milestoneEnum = @($chapterDef.properties.milestone.enum); $expectedEnum = @($null,'fv-calculator-complete','github-pages-published'); if (($milestoneEnum.Count -ne $expectedEnum.Count) -or (@(Compare-Object -ReferenceObject $expectedEnum -DifferenceObject $milestoneEnum -SyncWindow 0).Count -ne 0)) { throw 'schema milestone enum mismatch' }"</automated>
  </verify>
  <done>專案已有固定的 Node 驗證入口與 schema，後續 plan 可直接依既定欄位建立 manifest 與章節。</done>
</task>

<task type="auto">
  <name>Task 2: 建立 Phase 1 測試輔助與五個需求檢查檔</name>
  <files>tests/phase-01/helpers.mjs, tests/phase-01/manifest-schema.test.mjs, tests/phase-01/topology-order.test.mjs, tests/phase-01/chapter-nav.test.mjs, tests/phase-01/milestone-signaling.test.mjs, tests/phase-01/readme-entry.test.mjs</files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/phases/01-reader-entry-and-chapter-topology/01-RESEARCH.md
    .planning/phases/01-reader-entry-and-chapter-topology/01-VALIDATION.md
    tests/phase-01/helpers.mjs
    tests/phase-01/manifest-schema.test.mjs
    tests/phase-01/topology-order.test.mjs
    tests/phase-01/chapter-nav.test.mjs
    tests/phase-01/milestone-signaling.test.mjs
    tests/phase-01/readme-entry.test.mjs
  </read_first>
  <action>建立 `tests/phase-01/helpers.mjs`，提供 `readText(path)`, `readJson(path)`, `chapterFilesFromManifest(manifest)`, `extractBlock(text, startMarker, endMarker)` 四個可重用函式。建立 `tests/phase-01/manifest-schema.test.mjs`，測 `book/manifest.json` 可被 `book/manifest.schema.json` 接受，且 milestone chapterId 可解析到真實章節。建立 `tests/phase-01/topology-order.test.mjs`，檢查 manifest 的 `order` 為 `1..7` 連續整數，`file` 依序為 `chapters/01-reader-entry-and-first-win.md` 到 `chapters/07-publish-github-pages.md`，且 README 自動區塊內的章節清單順序與 manifest 完全一致。建立 `tests/phase-01/chapter-nav.test.mjs`，檢查每章都含 `<!-- CHAPTER_NAV_START -->` 與 `<!-- CHAPTER_NAV_END -->`，並至少有 `- 回首頁：[README](../README.md)`；第一章上一章固定為 `- 上一章：無`，最後一章下一章固定為 `- 下一章：無`。建立 `tests/phase-01/milestone-signaling.test.mjs`，檢查 `book/manifest.json` 僅有 `finish-fv-calculator` 與 `publish-github-pages` 兩章設 milestone，兩個章檔都要含有 `## 里程碑` heading，且 `README.md` 要含 `完成 FV 計算機` 與 `發布到 GitHub Pages`。建立 `tests/phase-01/readme-entry.test.mjs`，檢查 `README.md` 開頭 40 行內必須出現 `# Agent 使用電子書`, `完成可公開部署的 FV 計算機`, `開始閱讀`, `[第 1 章：建立你的閱讀與第一個實作起點](./chapters/01-reader-entry-and-first-win.md)`。</action>
  <acceptance_criteria>
    `tests/phase-01/helpers.mjs` 含有字串 `export function readText`, `export function readJson`, `export function extractBlock`
    `tests/phase-01/topology-order.test.mjs` 含有字串 `chapters/07-publish-github-pages.md`
    `tests/phase-01/chapter-nav.test.mjs` 含有字串 `CHAPTER_NAV_START` 與 `回首頁：[README](../README.md)`
    `tests/phase-01/milestone-signaling.test.mjs` 含有字串 `finish-fv-calculator` 與 `publish-github-pages`
    `tests/phase-01/readme-entry.test.mjs` 含有字串 `完成可公開部署的 FV 計算機`
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$files = 'tests/phase-01/helpers.mjs','tests/phase-01/manifest-schema.test.mjs','tests/phase-01/topology-order.test.mjs','tests/phase-01/chapter-nav.test.mjs','tests/phase-01/milestone-signaling.test.mjs','tests/phase-01/readme-entry.test.mjs'; foreach ($file in $files) { node --check $file }"</automated>
  </verify>
  <done>Phase 1 的每個 requirement 都有對應的自動化測試檔與可重用 helper，後續實作可以直接對著這些檢查完成。</done>
</task>

</tasks>

<verification>
先安裝 devDependencies，再確認 `node --check scripts/validate-manifest.mjs` 與所有 `tests/phase-01/*.test.mjs` 皆可被 Node 解析。這個 plan 不要求測試綠燈，因為 manifest / README / chapters 會在後續 plan 建立。
</verification>

<success_criteria>
`package.json`、schema、驗證腳本與五個測試檔都存在，且欄位與字串契約已鎖定；後續 plan 不需要再決定 manifest 格式、milestone key 或 README 檢查規則。
</success_criteria>

<output>
After completion, create `.planning/phases/01-reader-entry-and-chapter-topology/01-reader-entry-and-chapter-topology-01-SUMMARY.md`
</output>
